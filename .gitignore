.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
platformio.ini

# Build outputs — firmware binaries embed every -D build flag
# (WiFi PSK, MQTT creds, UPDATE_PASSWORD) as plaintext strings.
# Never commit these. Attach them to GitHub Releases only if
# they were built with non-secret credentials.
release/

# Subagent-driven-development scratch workspace
.superpowers/
