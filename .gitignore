.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
platformio.ini

# Build outputs. Firmware binaries no longer contain any WiFi, MQTT or
# updater credentials — those live in EEPROM, entered at provisioning time —
# so published binaries are safe. Ignored purely for repo size and hygiene:
# build artefacts are regenerated, and belong on GitHub Releases instead.
release/

# Subagent-driven-development scratch workspace
.superpowers/
